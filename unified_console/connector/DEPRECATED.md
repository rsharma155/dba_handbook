# Deprecated — replaced by native Go connector

The Java/JAR connector in this folder is **no longer used**.

Use:

```
unified_console/cmd/dba-console/   ← native Go binary (SQL Server + PostgreSQL)
unified_console/scripts/build.sh
unified_console/scripts/assemble-portable.sh
```

Portable distribution is now **~12 MB exe + HTML** with no `runtime/` folder.
